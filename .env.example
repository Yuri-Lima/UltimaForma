# Production template - Copy to .env.prod on VPS
# Fill all values before deployment

# API port (local dev: 3100)
PORT=3100

# Domain
DOMAIN=ultimaforma.id
API_SUBDOMAIN=api
APP_SUBDOMAIN=app
EMAIL_LETSENCRYPT=admin@ultimaforma.id

# Database (production)
DB_HOST=postgres
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=admin
DB_NAME=ultimaforma

# JWT (use strong secret in production - e.g. openssl rand -hex 64)
JWT_SECRET=9247a613944798908b4278c51ef4679e5b5ca8a04d2ad84b4e0edba563a6772ba196df46c6b4bf9d457df44c3d87fc40399b552b2fbffdfd29e451c18096ca27
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# MFA (32-byte hex for AES-256-GCM encryption of TOTP secrets)
MFA_SECRET_ENCRYPTION_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# Redis (production - use service name in docker-compose)
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=admin

# OpenAI (get key from https://platform.openai.com/api-keys)
OPENAI_API_KEY=""
