# Production template - Copy to .env.prod on VPS
# Fill all values before deployment

# Domain
DOMAIN=ultimaforma.id
API_SUBDOMAIN=api
APP_SUBDOMAIN=app
EMAIL_LETSENCRYPT=admin@ultimaforma.id

# Database (production)
DB_HOST=postgres
DB_PORT=5432
DB_USERNAME=
DB_PASSWORD=
DB_NAME=ultimaforma

# JWT (use strong secret in production)
JWT_SECRET=
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Redis (production - use service name in docker-compose)
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=

# OpenAI
OPENAI_API_KEY=
